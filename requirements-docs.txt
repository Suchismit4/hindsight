# Sphinx + runtime deps for autodoc on GitHub Actions (CPU JAX; no CUDA wheels).
# Keeps CI separate from requirements.txt (GPU stack).
# xarray-jax (import xarray_jax) pins jax/xarray/equinox; keep ranges aligned.
sphinx>=7,<9
sphinx-rtd-theme>=2,<4
jax[cpu]>=0.4.33,<0.5.0
equinox>=0.11.7,<0.12.0
xarray>=2024.9.0,<2025.0.0
xarray-jax>=0.0.5
numpy>=1.26,<3
scipy>=1.11
pandas>=2
PyYAML>=6
scikit-learn>=1.3
jsonschema>=4
tqdm>=4
threadpoolctl>=3
pyreadstat>=1.2
packaging
typing_extensions
